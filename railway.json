{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "preDeployCommand": "if [ -f release.sh ]; then bash release.sh; else echo 'No backend release context detected; skipping migrations'; fi"
  }
}
