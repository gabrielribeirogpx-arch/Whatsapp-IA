import ProviderCard from './ProviderCard';import ProviderForm from './ProviderForm';
export default function ProvidersTab(props:any){const {providers,...rest}=props;return <div className='space-y-4'><ProviderForm {...rest}/>{providers.length===0?<div className='settings-card p-8 text-center text-gray-600'>📭 Conecte sua conta WhatsApp Business oficial da Meta ou BSP.</div>:<div className='grid gap-2'>{providers.map((p:any)=><ProviderCard key={p.id} p={p} onTest={()=>props.onTest(p.id)} onActivate={()=>props.onActivate(p.id)} onDelete={()=>props.onDelete(p.id)} loading={props.loading}/>)}</div>}</div>}
